import React from 'react';

const Subtotal = () => {
  // Calculate the subtotal based on your application's logic
  const subtotalAmount = 100; // Replace with your actual subtotal calculation

  return (
    <div>
      <label>Subtotal:</label>
      <p>${subtotalAmount}</p>
    </div>
  );
};

export default Subtotal;
